# Technical Document ML Service

## Тема проекта

**Сервис извлечения и проверки данных из технической документации**

Пользователь загружает технические документы (PDF или изображения), ML-сервис извлекает из них структурированные данные, проводит валидацию входных данных и возвращает результат обработки. 

---

## Что реализовано на текущем этапе

### Задание №1 — объектная модель ML-сервиса

В проекте спроектирована доменная объектная модель, отражающая базовую бизнес-логику сервиса

Реализованы основные сущности предметной области:

- `User` — пользователь сервиса;
- `MLModel` — базовая ML-модель;
- `TechnicalDocumentExtractionModel` — ML-модель для обработки технической документации;
- `MLTask` — базовая ML-задача;
- `DocumentExtractionTask` — задача на обработку документов;
- `PredictionResult` — результат работы модели;
- `MLRequestHistoryRecord` — история запросов;
- `Transaction` — базовая транзакция;
- `CreditTransaction` — пополнение баланса;
- `DebitTransaction` — списание средств;
- `UploadedDocument` — загруженный документ;
- `ValidationIssue` — ошибка валидации входных данных.

### Задание №2 — структура проекта и Docker Compose

Проект приведен к воспроизводимой инфраструктурной структуре и подготовлен к запуску через Docker Compose

Описаны 4 сервиса:

- `app` — backend-приложение;
- `web-proxy` — reverse proxy на Nginx;
- `rabbitmq` — брокер сообщений;
- `database` — база данных PostgreSQL

---

### Задание №3 — хранение данных через ORM

К объектной модели подключена реальная база данных PostgreSQL с использованием ORM `SQLAlchemy`

#### Реализовано

**1 Подключение ORM и конфигурация БД**
- настроено подключение к PostgreSQL через env-переменные
- добавлены `SQLAlchemy`, `psycopg`, `python-dotenv`
- реализованы `engine`, `SessionLocal`, базовый `Base`

**2 ORM-модели и схема БД**
Описаны таблицы для ключевых сущностей предметной области:

- `users`
- `ml_models`
- `ml_tasks`
- `transactions`
- `uploaded_documents`
- `prediction_results`
- `ml_request_history`
- `task_documents`

Настроены:
- первичные ключи;
- внешние ключи;
- индексы;
- ограничения целостности;
- связи между сущностями

**3 Бизнес-операции поверх ORM**
Реализован прикладной слой сервисов:

- `user_service.py`
  - создание пользователя;
  - загрузка пользователя по `id`;
  - загрузка пользователя по `email`

- `billing_service.py`
  - пополнение баланса;
  - списание средств;
  - сохранение транзакций;
  - получение истории транзакций пользователя

- `history_service.py`
  - создание записи истории ML-запроса из задачи;
  - получение истории предиктов пользователя с сортировкой по дате

**4 Инициализация базы данных**
Реализован `init_db.py`, который:
- создает таблицы;
- добавляет демо-пользователя;
- добавляет демо-администратора;
- задает стартовый баланс;
- создает базовые ML-модели;
- выполняется автоматически при старте приложения;
- является идемпотентным

**5 Тестирование**
Добавлены тесты на основные сценарии:
- создание пользователя;
- загрузка пользователя из БД;
- пополнение баланса;
- списание баланса;
- ошибка при недостатке средств;
- сохранение истории транзакций;
- получение истории транзакций в обратном хронологическом порядке;
- создание истории ML-запросов;
- получение истории предиктов пользователя;
- идемпотентность `init_db`

Текущее состояние тестов:
- `14 passed`

---

### Задание №4 — REST API для ML-сервиса (FastAPI)

реализован REST API на `FastAPI`

#### Реализовано

**1 API-слой приложения**
Добавлен отдельный слой `api/` с роутерами, схемами запросов и ответов, а также централизованной обработкой ошибок

**2 Аутентификация и работа с пользователем**
Реализованы эндпоинты:
- `POST /auth/register` — регистрация пользователя;
- `POST /auth/login` — авторизация пользователя;
- `GET /users/me` — получение данных текущего пользователя

Для защищенных методов используется `Basic Auth`

**3 Работа с балансом**
Реализованы эндпоинты:
- `GET /balance` — просмотр текущего баланса пользователя;
- `POST /balance/top-up` — пополнение баланса

Операции пополнения сохраняются в истории транзакций

**4 Отправка данных на предсказание**
Реализован эндпоинт:
- `POST /predict` — отправка документов на ML-обработку

Что делает сценарий `predict`:
- принимает документы через `multipart/form-data`;
- сохраняет файлы в локальное хранилище;
- создает доменные объекты документов;
- создает и выполняет `DocumentExtractionTask`;
- списывает кредиты с баланса пользователя;
- сохраняет результат предсказания;
- сохраняет запись в истории ML-запросов;
- сохраняет транзакцию списания

**5 История операций**
Реализованы эндпоинты:
- `GET /history/transactions` — история транзакций пользователя;
- `GET /history/predictions` — история ML-запросов пользователя

**6 Валидация и ошибки**
- входные данные API валидируются через `Pydantic`;
- доменные исключения преобразуются в корректные HTTP-ответы;
- для ошибок используется единый JSON-формат ответа;
- добавлена централизованная регистрация exception handlers

**7 Управление сессиями**
Реализовано разделение зависимостей для:
- write-session — для эндпоинтов, изменяющих состояние;
- read-session — для read-only эндпоинтов

**8 Локальное хранение файлов**
Добавлен сервис хранения документов:
- сохранение входных файлов в директорию `storage/uploads`;
- генерация путей хранения;
- удаление файлов при ошибке выполнения сценария

**9 Тестирование**
Добавлены smoke API-тесты на основные сценарии:
- регистрация пользователя;
- авторизация;
- получение баланса;
- пополнение баланса;
- получение истории транзакций;
- `predict`;
- получение истории предсказаний

Текущее состояние тестов:
- `25 passed`

---

### Задание №5 — взаимодействие с ML-сервисом через RabbitMQ

В проекте реализован асинхронный сценарий взаимодействия с ML-сервисом через брокер сообщений `RabbitMQ`

#### Реализовано

**1 Асинхронная архитектура publisher -> broker -> consumers**
Синхронный сценарий обработки из задания №4 был разделен на два независимых use case:

- `submit` — прием запроса, сохранение документов, создание задачи в БД и публикация сообщения в RabbitMQ;
- `process` — обработка ранее поставленной задачи отдельным воркером

В результате приложение теперь работает по схеме:
- REST API принимает запрос;
- формирует ML-задачу;
- публикует сообщение в очередь;
- отдельные воркеры забирают сообщения и выполняют обработку

**2 Новый контракт POST /predict**
Эндпоинт `POST /predict` переведен в асинхронный режим:
- принимает документы через `multipart/form-data`;
- создает запись задачи и связи с документами;
- публикует сообщение в RabbitMQ;
- возвращает клиенту `202 Accepted` и `task_id`

Для жизненного цикла задачи добавлен новый статус:
- `queued` — задача принята API и поставлена в очередь на обработку

**3 RabbitMQ и очередь задач**
Добавлена инфраструктура обмена сообщениями:
- модуль `messaging/contracts.py` с JSON-контрактом сообщения;
- модуль `messaging/rabbitmq.py` для публикации и consumer-конфигурации;
- одна очередь `technical_document_prediction_tasks`;
- durable-очередь и persistent-сообщения (`delivery_mode=2`)

В сообщение передаются метаданные задачи:
- `task_id`;
- `user_id`;
- `model_name`;
- `timestamp`

**4 ML-воркеры**
Реализованы отдельные воркеры, которые:
- подключаются к RabbitMQ;
- подписываются на очередь;
- получают задачи из очереди;
- валидируют входные данные;
- выполняют mock-предикт;
- сохраняют результат обработки в базе данных;
- сохраняют историю ML-запросов;
- сохраняют транзакцию списания.

В `docker-compose.yml` добавлены два отдельных consumer-сервиса:
- `worker-1`;
- `worker-2`.

worker вынесен в отдельный сервисный слой:
- worker собирается из собственного Docker-контекста `./worker`;
- общий прикладной код подключается в worker-контейнер через volume:
  - `./app/src:/app/src:ro`

**5 ML-predict через Docling**
Вместо mock-предикта подключен inference backend:
- добавлен слой `inference/` с поддержкой backend обработки
- реализован `DoclingBackend`
- подготовлена архитектура для дальнейшего подлюкчения других backend обработчиков

Worker теперь обрабатывает PDF-документы через `Docling` и сохраняет артефакты обработки:
- `*.docling.json`;
- `*.docling.md`;
- `*.docling.txt`;
- `*.summary.json`;
- `task.summary.json`

Артефакты сохраняются в директорию: 
- `storage/artifacts/<task_id>/...`

**6 новый read API для задач и результатов**
- `GET /tasks` - список задач пользователя + фильтрация по статусу;
- `GET /tasks/{task_id}` - детальная информация по задаче;
- `GET /tasks/{task_id}/result` - задача, результат обработки и список артефактов;

**7 Устойчивость и корректность обработки**
Добавлены механизмы надежности:
- healthcheck для `rabbitmq` и `database`;
- ожидание готовности сервисов через `depends_on.condition: service_healthy`;
- retry-подключение воркера к RabbitMQ;
- `basic_ack` только после успешной обработки;
- `basic_nack(requeue=False)` для битых сообщений;
- защита от повторной обработки уже завершенных задач;
- блокировка `SELECT ... FOR UPDATE` при обработке задачи в воркере

**8 Разделение сервисов и общие volume**  
API и worker работают в отдельных контейнерах:
- общий прикладной код подключается в worker через volume:
  - `./app/src:/app/src:ro`;
- входные документы доступны для `app`, `worker-1` и `worker-2` через volume:
  - `./storage/uploads:/app/storage/uploads`
- артефакты обработки доступны через volume:
  - `./storage/artifacts:/app/storage/artifacts`

**9 Ручное тестирование сценария**
Проведена ручная проверка end-to-end сценария:
- через Swagger UI отправлена задача на `POST /predict`;
- задача получила `task_id` и статус `queued`;
- сообщение появилось в RabbitMQ;
- один из воркеров обработал задачу через `Docling`;
- задача перешла в статус `completed`;
- в файловом хранилище появились артефакты обработки;
- результат стал доступен через `GET /tasks/{task_id}` и `GET /tasks/{task_id}/result`
- в истории предсказаний и транзакций появились соответствующие записи

**10 Тестирование**
Обновлены и добавлены тесты:
- submit-сценарий `POST /predict` с ответом `202 Accepted`;
- проверка, что задача сохраняется в статусе `queued`;
- проверка публикации сообщения через spy вместо реального RabbitMQ;
- тесты processing-сервиса;
- тест на идемпотентность повторной обработки завершенной задачи;
- тесты inference- слоя;
- тесты `DoclingBackend`;
- тесты read API;

Текущее состояние тестов:
- `38 passed`

---

### Задание №6 — Web-интерфейс личного кабинета ML-сервиса

В проекте реализован Web-интерфейс личного кабинета пользователя поверх уже существующего backend-приложения.

#### Реализовано

**1 Web-слой приложения**
добавлен отдельный слой `web/`, который работает внутри основного backend runtime `app` и использует серверный рендеринг HTML через шаблоны

В Web-слое реализованы:
- `web/routers/pages.py` — страницы интерфейса;
- `web/routers/actions.py` — обработчики Web-форм;
- `web/deps.py` — зависимости для Web-страниц;
- `web/templates/` — HTML-шаблоны;
- `web/static/` — стили и JavaScript

**2 Аутентификация для WebUI**
для браузерного интерфейса добавлена JWT-аутентификация через `HttpOnly cookie`

Реализовано:
- выдача JWT после входа;
- хранение access token в cookie;
- logout с очисткой cookie;
- поддержка нескольких схем аутентификации:
  - `Basic Auth` — для API и тестов;
  - `Bearer token` — для API-клиентов;
  - `JWT cookie` — для WebUI

**3 Безопасность Web-форм**
в WebUI добавлена базовая защита от CSRF через проверку `Origin/Referer` для всех изменяющих состояние POST-форм

**4 Реализованные страницы WebUI**
добавлены страницы:
- `/` — главная страница;
- `/login` — вход;
- `/register` — регистрация;
- `/dashboard` — личный кабинет;
- `/balance-ui` — просмотр и пополнение баланса;
- `/predict-ui` — создание ML-задачи;
- `/tasks-ui` — список задач пользователя;
- `/tasks-ui/{task_id}` — детальная страница задачи;
- `/history-ui` — история операций и предсказаний

**5 Личный кабинет пользователя**
в Web-интерфейсе пользователь может:
- просматривать данные своего аккаунта;
- видеть текущий баланс;
- пополнять баланс;
- отправлять документы на ML-обработку;
- просматривать список задач;
- открывать детальную страницу задачи;
- просматривать историю транзакций и предсказаний

**6 Интеграция WebUI с асинхронным ML-сценарием**
WebUI работает поверх уже реализованного асинхронного backend-сценария:
- форма на `/predict-ui` отправляет документы в backend;
- backend создает задачу и публикует сообщение в RabbitMQ;
- отдельный worker выполняет обработку;
- пользователь отслеживает результат на странице `/tasks-ui/{task_id}`.

Для задач в статусах `queued` и `processing` предусмотрено автообновление страницы

**7 Улучшение UX**
в интерфейсе дополнительно реализовано:
- отображение текущего баланса на странице создания задачи;
- отображение стоимости выбранной модели;
- предварительная UI-подсказка о достаточности баланса;
- более наглядное отображение результата обработки, validation issues и артефактов

**8 Тестирование**
добавлены WebUI-тесты на ключевые пользовательские сценарии:
- открытие главной страницы;
- регистрация и вход через WebUI;
- logout;
- защита кабинета от неаутентифицированного пользователя;
- просмотр страницы баланса;
- пополнение баланса через Web-форму;
- открытие страницы истории;
- открытие страницы создания задачи;
- ошибка отправки формы без документов;
- успешная отправка задачи;
- открытие списка задач;
- фильтрация задач;
- открытие детальной страницы задачи;

Текущее состояние тестов:
- `59 passed`

---

### Задание №7 — тестирование работоспособности системы

в проекте добавлены тесты, которые позволят проверить сквозные пользовательские сценарии: работа с пользователем и балансом, постановка ML-задачи, обработка результата и проверки истории операций 

#### Что было в проекте до задания №7

до этого этапа в проекте уже был сформирован базовый тестовый контур:
- тесты сервисного слоя на работу с пользователями, балансом и историей;
- API-тесты на регистрацию, авторизацию, баланс, историю, predict и задачи;
- тесты processing-сценария асинхронной обработки;
- WebUI-тесты на страницы, вход, пополнение баланса, отправку задач и просмотр результатов

#### Что реализовано в рамках задания №7

добавлен отдельный набор верхнеуровневых сценарных тестов, который проверяет критичные пользовательские пути:

**1 полный успешный пользовательский сценарий**
- регистрация пользователя;
- авторизация;
- получение начального баланса;
- пополнение баланса;
- отправка ML-задачи;
- обработка задачи;
- получение результата;
- проверка истории транзакций и истории предсказаний

**2 повторная авторизация**
- повторный вход пользователя с корректными учетными данными;
- подтверждение, что доступ к защищенным эндпоинтам сохраняется

**3 недостаточный баланс**
- попытка отправить ML-задачу при нехватке средств;
- проверка, что задача не создается и не ставится в очередь;
- проверка, что баланс не изменяется;
- проверка, что не создаются транзакции и записи истории

**4 ошибка ML-обработки**
- искусственный сбой backend-обработки;
- перевод задачи в статус `failed`;
- проверка, что деньги не списываются;
- проверка, что результат обработки и успешная история не создаются

#### Что подтверждают эти тесты

добавленные сценарии подтверждают, что система корректно работает как единое целое и соблюдает ключевые бизнес-инварианты:
- пользователь может пройти полный путь от регистрации до получения результата;
- пополнение и списание баланса происходят только в корректных сценариях;
- при недостаточном балансе задача не запускается;
- при ошибке ML-обработки не происходит ошибочного списания средств;
- история транзакций и история предсказаний согласованы с выполненными действиями пользователя

#### Текущее состояние тестов

После добавления сценарных тестов общее состояние тестового набора:
- `63 passed`

---

## Текущая структура проекта

```text
technical-document-ml-service/
├── app/
│   ├── .env
│   ├── .dockerignore
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── src/
│   │   └── technical_document_ml_service/
│   │       ├── __init__.py
│   │       ├── main.py
│   │       ├── api/
│   │       │   ├── __init__.py
│   │       │   ├── deps.py
│   │       │   ├── errors.py
│   │       │   ├── routers/
│   │       │   │   ├── __init__.py
│   │       │   │   ├── auth.py
│   │       │   │   ├── balance.py
│   │       │   │   ├── health.py
│   │       │   │   ├── history.py
│   │       │   │   ├── predict.py
│   │       │   │   ├── tasks.py
│   │       │   │   └── users.py
│   │       │   └── schemas/
│   │       │       ├── __init__.py
│   │       │       ├── auth.py
│   │       │       ├── balance.py
│   │       │       ├── common.py
│   │       │       ├── history.py
│   │       │       ├── predict.py
│   │       │       ├── tasks.py
│   │       │       └── users.py
│   │       ├── core/
│   │       │   ├── __init__.py
│   │       │   ├── config.py
│   │       │   └── security.py
│   │       ├── db/
│   │       │   ├── __init__.py
│   │       │   ├── base.py
│   │       │   ├── config.py
│   │       │   ├── init_db.py
│   │       │   ├── models.py
│   │       │   └── session.py
│   │       ├── domain/
│   │       │   ├── __init__.py
│   │       │   ├── entities.py
│   │       │   ├── enums.py
│   │       │   └── exceptions.py
│   │       ├── inference/
│   │       │   ├── __init__.py
│   │       │   ├── contracts.py
│   │       │   ├── exceptions.py
│   │       │   ├── registry.py
│   │       │   ├── selector.py
│   │       │   └── backends/
│   │       │       ├── __init__.py
│   │       │       ├── base.py
│   │       │       ├── docling_backend.py
│   │       │       └── marker_backend.py
│   │       ├── messaging/
│   │       │   ├── __init__.py
│   │       │   ├── contracts.py
│   │       │   └── rabbitmq.py
│   │       ├── services/
│   │       │   ├── __init__.py
│   │       │   ├── auth_service.py
│   │       │   ├── billing_service.py
│   │       │   ├── document_storage_service.py
│   │       │   ├── dto.py
│   │       │   ├── history_service.py
│   │       │   ├── inference_mappers.py
│   │       │   ├── mappers.py
│   │       │   ├── orm_queries.py
│   │       │   ├── prediction_processing_service.py
│   │       │   ├── prediction_service.py
│   │       │   ├── prediction_submission_service.py
│   │       │   ├── task_query_service.py
│   │       │   └── user_service.py
│   │       ├── web/
│   │       │   ├── __init__.py
│   │       │   ├── deps.py
│   │       │   ├── model_catalog.py
│   │       │   ├── security.py
│   │       │   ├── templating.py
│   │       │   ├── routers/
│   │       │   │   ├── __init__.py
│   │       │   │   ├── actions.py
│   │       │   │   └── pages.py
│   │       │   ├── static/
│   │       │   │   ├── css/
│   │       │   │   │   └── app.css
│   │       │   │   └── js/
│   │       │   │       └── task_polling.js
│   │       │   └── templates/
│   │       │       ├── balance.html
│   │       │       ├── base.html
│   │       │       ├── dashboard.html
│   │       │       ├── history.html
│   │       │       ├── index.html
│   │       │       ├── login.html
│   │       │       ├── predict.html
│   │       │       ├── register.html
│   │       │       ├── task_detail.html
│   │       │       └── tasks.html
│   │       └── workers/
│   │           ├── __init__.py
│   │           └── prediction_worker.py
│   └── tests/
│       ├── conftest.py
│       ├── test_api_auth.py
│       ├── test_api_balance.py
│       ├── test_api_history.py
│       ├── test_api_predict.py
│       ├── test_api_tasks.py
│       ├── test_billing_service.py
│       ├── test_docling_backend.py
│       ├── test_history_service.py
│       ├── test_inference_registry.py
│       ├── test_init_db.py
│       ├── test_prediction_processing_service.py
        ├── test_system_flows.py
│       ├── test_user_service.py
│       ├── test_web_auth.py
│       ├── test_web_pages.py
│       └── test_web_predict.py
├── worker/
│   ├── Dockerfile
│   └── requirements.txt
├── web-proxy/
│   ├── Dockerfile
│   └── nginx.conf
├── storage/
│   ├── artifacts/
│   ├── rabbitmq/
│   └── uploads/
├── docker-compose.yml
├── pyproject.toml
├── README.md
└── .gitignore
```

---

## Запуск проекта
Сборка и запуск сервисов:
`docker compose up -d --build`

Проверка работоспособности приложения:
`curl http://localhost/health`

Ожидаемый результат:
`{"status":"ok"}`

## Запуск тестов
Тесты запускаются внутри контейнера `app`:
`docker compose run --rm app pytest -q`

Ожидаемый результат на данном этапе:
`59 passed`

## RabbitMQ UI
доступен по адресу:
`http://localhost:15672`

по умолчанию:
- login: `guest`
- password: `guest`

## Swagger UI
доступен по адресу:
`http://localhost/docs`

## Web UI
доступен по адресу:
`http://localhost/`
